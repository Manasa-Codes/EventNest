<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Event Posting</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        :root {
            --primary-color: #c21212;
            --secondary-color: #bd7028;
            --dark-color: #4b5157;
            --light-color: #f8f9fa;
        }
        
        body {
            background: linear-gradient(135deg, #a09d9d, #1a1a2e);
            min-height: 100vh;
            
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }
        
        .form-container {
            margin-top: 30px;
            /* animation: fadeIn 0.8s ease-in-out; */
        }
        
        .card {
            background-color: rgba(33, 37, 41, 0.9);
            color: white;
            border: none;
            border-radius: 15px;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.5);
            backdrop-filter: blur(5px);
            overflow: hidden;
        }
        
        .card-header {
            border-bottom: 1px solid rgba(255, 255, 255, 0.1);
            background: linear-gradient(to right, var(--primary-color), var(--secondary-color));
        }
        
        h2 {
            font-weight: 700;
            color: white;
            margin: 0;
            padding: 20px 0;
        }
        
        form {
            padding: 20px;
        }
        
        label {
            display: block;
            margin-bottom: 8px;
            font-weight: 500;
            font-size: larger;
            color: var(--secondary-color);
        }
        
        input[type="text"],
        input[type="date"],
        textarea {
            width: 100%;
            padding: 12px 15px;
            margin-bottom: 20px;
            border: 1px solid rgba(255, 255, 255, 0.1);
            border-radius: 8px;
            background-color: rgba(155, 153, 153, 0.3);
            color: white;
            transition: all 0.3s ease;
            font-size: 16px;
        }
        
        input[type="text"]:focus,
        input[type="date"]:focus,
        textarea:focus {
            outline: none;
            border-color: var(--primary-color);
            box-shadow: 0 0 0 3px rgba(108, 92, 231, 0.2);
            background-color: rgba(177, 166, 166, 0.5);
        }
        
        textarea {
            min-height: 120px;
            resize: vertical;
        }
        
        .btn-primary {
            background: linear-gradient(to right, var(--primary-color), var(--secondary-color));
            border: none;
            padding: 12px;
            font-weight: 600;
            letter-spacing: 1px;
            text-transform: uppercase;
            border-radius: 8px;
            transition: all 0.3s ease;
            margin-top: 10px;
        }
        
        .btn-primary:hover {
            transform: translateY(-2px);
            box-shadow: 0 5px 15px rgba(108, 92, 231, 0.4);
        }
        
        ::placeholder {
            color: rgba(255, 255, 255, 0.5);
            opacity: 1;
        }
        
        @keyframes fadeIn {
            from {
                opacity: 0;
                transform: translateY(20px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }
        
        /* Responsive adjustments */
        @media (max-width: 576px) {
            .form-container {
                margin-top: 40px;
                padding: 0 15px;
            }
            
            .card {
                border-radius: 10px;
            }
        }
    </style>
</head>

<body>
    <div class="container d-flex justify-content-center align-items-center form-container">
        <div class="card p-4 shadow" style="width: 100%; max-width: 750px;">
            <div class="text-center">
                <h2 style="color: #c21212;">POST AN EVENT</h2>
            </div>
            <form action="/event" method="post">
                <div class="mb-3">
                    <label for="name">Event Name:</label>
                    <input type="text" id="name" name="name" placeholder="Enter event name" required>
                </div>
                <div class="mb-3">
                    <label for="date">Event Date:</label>
                    <input type="date" id="date" name="date" placeholder="Enter event date" required>
                </div>
                <div class="mb-3">
                    <label for="venue">Event Venue:</label>
                    <input type="text" id="venue" name="venue" placeholder="Enter event venue" required>
                </div>
                <div class="mb-3">
                    <label for="description" class="form-label">Event Description</label>
                    <textarea id="description" name="description" placeholder="Enter event details" required></textarea>
                </div>
                <div class="mb-3">
                    <label for="rules" class="form-label">Rules and  Regulations</label>
                    <textarea id="rules" name="rules" placeholder="Rules" required></textarea>
                </div>
                
                <button type="submit" class="btn btn-primary w-100">Submit Event</button>
            </form>
        </div>
    </div>
    
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>