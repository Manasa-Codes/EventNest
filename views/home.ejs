



<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Event Portal | College Events Hub</title>
    
    <!-- Bootstrap 5 CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- Google Fonts -->
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&family=Montserrat:wght@700&display=swap" rel="stylesheet">
    <!-- Font Awesome -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">

    <style>
        :root {
            --primary-red: #8b070e;
            --dark-red: #b20710;
            --black: #000000;
            --dark-gray: #474545;
            --light-gray: #f5f5f5;
        }
        
        body {
            background: linear-gradient(rgba(0, 0, 0, 0.8), rgba(0, 0, 0, 0.8)), url('/homebg.jpg');
            background-size: cover;
            background-attachment: fixed;
            background-position: center;
            min-height: 100vh;
            margin: 0;
            display: flex;
            flex-direction: column;
            font-family: 'Poppins', sans-serif;
            color: var(--light-gray);
        }
        
        .content {
            flex-grow: 1;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            text-align: center;
            padding: 2rem;
            animation: fadeIn 1s ease-in-out;
        }
        
        h1 {
            font-family: 'Montserrat', sans-serif;
            font-size: 3.5rem;
            font-weight: 700;
            margin-bottom: 1.5rem;
            text-shadow: 2px 2px 8px rgba(0,0,0,0.5);
            color: var(--primary-red);
        }
        
        .lead {
            font-size: 1.5rem;
            max-width: 700px;
            margin-bottom: 3rem;
            opacity: 0.9;
        }
        
        .login-section {
            padding: 2.5rem;
            background: rgba(20, 20, 20, 0.9);
            border-radius: 16px;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.5);
            max-width: 800px;
            width: 90%;
            margin: 2rem auto;
            border: 1px solid var(--primary-red);
        }
        
        .login-section h2 {
            color: var(--light-gray);
            margin-bottom: 2rem;
            font-weight: 600;
            position: relative;
            padding-bottom: 0.5rem;
        }
        
        .login-section h2::after {
            content: '';
            position: absolute;
            bottom: 0;
            left: 50%;
            transform: translateX(-50%);
            width: 80px;
            height: 3px;
            background: var(--primary-red);
            border-radius: 3px;
        }
        
        .btn-login {
            padding: 0.8rem 2rem;
            border-radius: 50px;
            font-weight: 500;
            letter-spacing: 0.5px;
            transition: all 0.3s ease;
            margin: 0.5rem;
            min-width: 200px;
            position: relative;
            overflow: hidden;
            border: none;
        }
        
        .btn-user {
            background-color: var(--primary-red);
        }
        
        .btn-admin {
            background-color: var(--dark-gray);
            border: 1px solid var(--primary-red);
        }
        
        .btn-login:hover {
            transform: translateY(-3px);
            box-shadow: 0 8px 20px rgba(229, 9, 20, 0.3);
        }
        
        .btn-login::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, transparent, rgba(255,255,255,0.2), transparent);
            transition: 0.5s;
        }
        
        .btn-login:hover::before {
            left: 100%;
        }
        
        .icon {
            margin-right: 0.5rem;
        }
        
        .features {
            display: flex;
            justify-content: center;
            flex-wrap: wrap;
            gap: 2rem;
            margin: 3rem 0;
            max-width: 1000px;
        }
        
        .feature-card {
            background: rgba(30, 30, 30, 0.8);
            border-radius: 12px;
            padding: 1.5rem;
            width: 220px;
            transition: all 0.3s ease;
            border: 1px solid rgba(229, 9, 20, 0.3);
        }
        
        .feature-card:hover {
            transform: translateY(-10px);
            background: rgba(40, 40, 40, 0.9);
            box-shadow: 0 10px 25px rgba(229, 9, 20, 0.2);
            border-color: var(--primary-red);
        }
        
        .feature-icon {
            font-size: 2.5rem;
            margin-bottom: 1rem;
            color: var(--primary-red);
        }
        
        .feature-title {
            font-weight: 600;
            margin-bottom: 0.5rem;
            color: white;
        }
        
        .feature-desc {
            font-size: 0.9rem;
            opacity: 0.8;
        }
        
        @keyframes fadeIn {
            from {
                opacity: 0;
                transform: translateY(20px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }
        
        @media (max-width: 768px) {
            h1 {
                font-size: 2.5rem;
            }
            
            .lead {
                font-size: 1.2rem;
            }
            
            .login-section {
                padding: 1.5rem;
            }
            
            .btn-container {
                flex-direction: column;
            }
            
            .btn-login {
                width: 100%;
                margin: 0.5rem 0;
            }
        }
    </style>
</head>

<body>
    <div class="content">
        <h1>Welcome to Event Portal</h1>
        <p class="lead">Your central hub for all college events and activities</p>
        
        
    </div>

    <div class="login-section">
        <h2>Get Started</h2>
        <div class="d-flex flex-column flex-md-row justify-content-center align-items-center btn-container">
            <a href="/login" class="btn btn-login btn-user">
                <i class="fas fa-user-graduate icon"></i>Student Login
            </a>
            <a href="/admin-login" class="btn btn-login btn-admin">
                <i class="fas fa-user-shield icon"></i>Admin Login
            </a>
        </div>
    </div>

    <!-- Bootstrap 5 JS Bundle -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>